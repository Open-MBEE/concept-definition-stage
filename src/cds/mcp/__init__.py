"""cds.mcp — MCP tool boundary (tier T5 / constraint K1).

The LLM's ENTIRE reachable surface. Serves only the whitelisted tools; there is no
exec/file/network/shell tool. See docs/architecture/cds-web-app.md sections 8.1 and 5.3.
"""
